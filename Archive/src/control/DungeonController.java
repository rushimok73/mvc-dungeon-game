package control;

/**
 * Interface for controller, which starts the controller.
 */
public interface DungeonController {
  /**
   * Starts the controller up, and enables users to give commands.
   */
  void playGame();
}
